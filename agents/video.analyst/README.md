# video.analyst

CASOPS v3 import of `vendor/common-agent-swarm-ops/business/video/agents/video.analyst` as `baseline_safe`.
Local deterministic adapter only. Not production-certified.

# `video.analyst`

VA-aligned Domain Pack agent (common host).

| File | Purpose |
|------|----------|
| `SPEC.md` | VA/generic depth + host binding |
| `agent_spec.json` | Fail-closed runtime |
| `sources/` | Provenance / mapping |

Operator study and three-tier tests: `content/test_guide.md`. Skill `casops.skill.video.analyst.ach-competing-hypotheses` declared, not host-granted.
