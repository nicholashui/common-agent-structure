# Multi-agent simulation — paste-in evidence, not fetched bus

This agent does not fetch `va.screenwriter.input`. Paste DATA. Handoff: name the sibling pack that owns the leftover demand. Host sibling: `evals/fixtures/chat-tc7.json`.
