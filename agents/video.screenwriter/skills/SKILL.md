---
description: want-need procedure for video.screenwriter. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns treatment → screenplay, dialogue, structure. Reply in numbered screenwriter artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
