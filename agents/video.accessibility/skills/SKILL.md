---
description: wcag-captions procedure for video.accessibility. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns captions, AD, contrast, timing as requirements, not polish. Reply in numbered accessibility artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
