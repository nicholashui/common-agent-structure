---
description: hands-face-identity procedure for video.aiqaconsistency. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns identity/temporal consistency checks, not the generate step. Reply in numbered aiqaconsistency artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
