---
description: llm-as-judge procedure for video.judge. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns accept/refine/escalate against a rubric, not a review essay. Reply in numbered judge artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
