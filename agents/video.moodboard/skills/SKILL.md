---
description: each-still-why procedure for video.moodboard. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns reference clusters with rights notes, not finished frames. Reply in numbered moodboard artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
