---
description: location-path procedure for video.travelcine. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns place-as-character travel image, not real-estate listing photos. Reply in numbered travelcine artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
