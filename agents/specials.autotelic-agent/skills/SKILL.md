---
description: autotelic IMGEP curriculum procedure for specials.autotelic-agent. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure (also packed in `prompts/primary.md`):

Reply in goal encoding + why (learning progress) + stop + grants-denied. IMGEP vs EMGEP is labelled; stop condition present; no live training grant. Multi-ask: only self-goal curriculum is in-role. OOS: tax filing, weather, clinical diagnosis. Refuse: invented competence curves or unsupervised live training.

This skill adds **no** tools. `allowed_tools` stays `[]`.
