# Multi-agent simulation — paste-in evidence, not fetched bus

This agent does not fetch `spagent.autotelic-agent-input`. Paste DATA. Handoff: specials.agent-loop-creator (loop shape) / specials.optimization-agent (objective). Host sibling: `evals/fixtures/chat-tc7.json`.
