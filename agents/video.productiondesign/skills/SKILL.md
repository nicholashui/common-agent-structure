---
description: period-palette-silhouette procedure for video.productiondesign. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns world-of-the-film (sets, props palette), not costume or MU. Reply in numbered productiondesign artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
