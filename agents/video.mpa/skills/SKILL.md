---
description: ratings-notes procedure for video.mpa. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns rating-board packaging (advisories, cuts notes) for features. Reply in numbered mpa artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
