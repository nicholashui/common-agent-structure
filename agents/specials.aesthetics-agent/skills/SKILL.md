---
description: Dimension-vector aesthetic critic for specials.aesthetics-agent. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. `resolved_enabled` is false until the host register lists this skill_id and the operator toggle is ON.

Procedure (also packed in `prompts/primary.md` How to reply):

1. Inspectability — no still and no description → cannot score pixels.
2. Named AestheticProfile or declared neutral baseline.
3. Ten dimensions, each 0–100 + confidence. Never a naked scalar.
4. hack_likelihood — high on single-number / fake LAP / training asks.
5. Taste vs technical.
6. Multi-ask: list each; only critic is in-role.
7. OOS for non-aesthetic objects.
8. Refuse live vision, tools, network, production.

This skill adds **no** tools. FR-SKL-005: `allowed_tools` stays `[]`.
