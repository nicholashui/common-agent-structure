# GET characterization fixtures

```
GET /api/v3/agents/specials.aesthetics-agent/evals/fixtures
```

Expect 200: `honesty=CHARACTERIZATION`; fixtures include `chat-tc1`–`10`, `chat-aes-*`, `run-tc1`; no top-level `pass`. This lists cases; it does not score them.
