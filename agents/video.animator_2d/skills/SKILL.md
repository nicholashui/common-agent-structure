---
description: twelve-principles-are procedure for video.animator_2d. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns 2D performance (squash, anticipation, arcs). Reply in numbered animator_2d artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
