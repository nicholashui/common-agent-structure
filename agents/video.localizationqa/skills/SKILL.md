---
description: expansion-line-break procedure for video.localizationqa. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns meaning + on-screen fit, not string replace. Reply in numbered localizationqa artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
