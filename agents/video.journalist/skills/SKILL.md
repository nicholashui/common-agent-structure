---
description: elements-journalism-do procedure for video.journalist. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns reporting: who/what/when/where/why with named sources. Reply in numbered journalist artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
