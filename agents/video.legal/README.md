# video.legal

CASOPS v3 import of `vendor/common-agent-swarm-ops/business/video/agents/video.legal` as `baseline_safe`.
Local deterministic adapter only. Not production-certified.

# `video.legal`

VA-aligned Domain Pack agent (common host).

| File | Purpose |
|------|----------|
| `SPEC.md` | VA/generic depth + host binding |
| `agent_spec.json` | Fail-closed runtime |
| `sources/` | Provenance / mapping |

Operator study and three-tier tests: `content/test_guide.md`. Skill `casops.skill.video.legal.four-factor` declared, not host-granted.
