---
description: fadgi-iasa-as procedure for video.archivemaster. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns preservation master specs (format, checksum, color). Reply in numbered archivemaster artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
