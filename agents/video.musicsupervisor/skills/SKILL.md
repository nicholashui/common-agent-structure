---
description: two-licenses-composition procedure for video.musicsupervisor. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns licensed music (sync + master), not original score. Reply in numbered musicsupervisor artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
