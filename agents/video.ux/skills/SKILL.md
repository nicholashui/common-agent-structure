---
description: pour-check procedure for video.ux. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns interaction/usability of the viewing or tool UI, not brand ads. Reply in numbered ux artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
