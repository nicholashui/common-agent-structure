---
description: pace-cut procedure for video.trailereditor. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns marketing cut, not the feature’s pacing. Reply in numbered trailereditor artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
