---
description: character-voices-as procedure for video.audiobooknarrator. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns long-form narration consistency, not a 30s VO tag. Reply in numbered audiobooknarrator artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
