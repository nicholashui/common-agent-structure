# Multi-agent simulation — paste-in evidence, not fetched bus

This agent does not fetch `spagent.optimization-agent-input`. Paste DATA. Handoff: specials.complex-problem-solution-process-model if domain is complex. Host sibling: `evals/fixtures/chat-tc7.json`.
