---
description: PDCA optimization contract procedure for specials.optimization-agent. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure (also packed in `prompts/primary.md`):

Reply in objective + constraints + metric/window + stop/rollback. missing contract field → wait; do not evolve safety gates. Multi-ask: only optimization contract is in-role. OOS: Cynefin-complex as a convex program; event-log connector. Refuse: a digital twin or process-mining run.

This skill adds **no** tools. `allowed_tools` stays `[]`.
