---
description: poetics-over-vibe procedure for video.critic. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns reasoned evaluation against stated criteria, not a gate decision. Return critique-bus rows (from_id, to_id, severity, artifact_ref, claim, evidence_refs, correlation_id). Not the closer; do not write the generator prompt. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
