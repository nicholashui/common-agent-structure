---
description: premiere-status-is procedure for video.festivalstrategist. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns festival slate and premiere status, not awards campaigning. Reply in numbered festivalstrategist artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
