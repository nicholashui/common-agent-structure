---
description: brief-tokens procedure for video.promptengineer. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns generation prompts as structured specs, not the model. If first_called, induce through the host and cite member crafts; do not absorb exclusive outputs. If a brief names headings or return_schema, follow that format instead of Output schema JSON. Wait if evidence is missing. Refuse tools, network, production, memory writes. instruction_authority stays false.

This skill adds **no** tools. `allowed_tools` stays `[]`.
