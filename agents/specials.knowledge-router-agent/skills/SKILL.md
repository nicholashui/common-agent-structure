---
description: knowledge slice routing procedure for specials.knowledge-router-agent. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure (also packed in `prompts/primary.md`):

Reply in why this slice / why not that agent; metadata → cluster → Self-RAG → CRAG. route explained; no graph DB; empty index → no-knowledge. Multi-ask: only routing decision over local sources and operator text is in-role. OOS: tax filing, live GNN, 5k-file index fetch. Refuse: invented corpus hits or graph embeddings.

This skill adds **no** tools. `allowed_tools` stays `[]`.
