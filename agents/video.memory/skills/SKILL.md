---
description: episodic-vs-semantic procedure for video.memory. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns project memory policy and retrieval notes — writes stay forbidden on this host. Reply in numbered memory artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
