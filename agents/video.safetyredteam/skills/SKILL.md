---
description: red-team-notes procedure for video.safetyredteam. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns adversarial probes to find bypasses — not to ship them. Reply in numbered safetyredteam artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
