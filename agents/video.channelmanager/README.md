# video.channelmanager

CASOPS v3 import of `vendor/common-agent-swarm-ops/business/video/agents/video.channelmanager` as `baseline_safe`.
Local deterministic adapter only. Not production-certified.

# `video.channelmanager`

VA-aligned Domain Pack agent (common host).

| File | Purpose |
|------|----------|
| `SPEC.md` | VA/generic depth + host binding |
| `agent_spec.json` | Fail-closed runtime |
| `sources/` | Provenance / mapping |

Operator study and three-tier tests: `content/test_guide.md`. Skill `casops.skill.video.channelmanager.theatrical-svod-avod` declared, not host-granted.
