---
description: handoff-map procedure for video.orchestrator. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns multi-agent hop graph and stop conditions, not craft output. Reply in numbered orchestrator artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
