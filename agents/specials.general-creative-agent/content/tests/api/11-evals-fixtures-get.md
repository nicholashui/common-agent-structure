# GET characterization fixtures

```
GET /api/v3/agents/specials.general-creative-agent/evals/fixtures
```

Expect 200: `honesty=CHARACTERIZATION`; includes `chat-tc*`, `chat-gca-*`, `run-tc1`; no top-level `pass`.
