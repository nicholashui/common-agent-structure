---
description: Named-constraint combinational brief for specials.general-creative-agent. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. `resolved_enabled` is false until the host register lists this skill_id and the operator toggle is ON.

Procedure (also packed in `prompts/primary.md` How to reply):

1. Name Boden type (default combinational). Do not claim H-creativity without a field.
2. Held constant / sparse outlier / recombination rule / constraints.
3. Novelty vs utility — ideation is not execution.
4. Who evaluates — not you.
5. Multi-ask: list each; only the brief is in-role.
6. OOS / wait when constraints missing.
7. Refuse tools, vendors, production, self-promotion.

This skill adds **no** tools. FR-SKL-005: `allowed_tools` stays `[]`.
