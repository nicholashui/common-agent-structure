---
description: move-notes procedure for video.cameraoperator. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Executes camera moves; does not own lighting design. One motor only; no push/pull/pan/orbit unless the brief grants it. If a brief names headings, follow them instead of Output schema JSON. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
