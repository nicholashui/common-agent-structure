---
description: retention-curve procedure for video.retentionoptimizer. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns drop-off hypotheses, not the edit lock. Reply in numbered retentionoptimizer artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
