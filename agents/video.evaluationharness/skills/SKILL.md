---
description: vbench-16-dimensions procedure for video.evaluationharness. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns running a named benchmark protocol, not inventing scores. Reply in numbered evaluationharness artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
