---
description: gagn-events-merrill procedure for video.instructionaldesign. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns learning objectives and assessment alignment, not LMS plumbing. Reply in numbered instructionaldesign artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
