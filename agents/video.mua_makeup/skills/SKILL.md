---
description: continuity-stills-per procedure for video.mua_makeup. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns makeup/hair continuity, not costume. Bare-makeup lock only; do not author the pore manifesto. If a brief names headings, follow them instead of Output schema JSON. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
