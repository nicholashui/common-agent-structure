---
description: vbench-16-dimensions procedure for video.benchmarkresearch. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns benchmark maps and staleness, not running the harness. Reply in numbered benchmarkresearch artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
