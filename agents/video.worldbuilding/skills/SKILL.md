---
description: geography-institutions-causal procedure for video.worldbuilding. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns consistent secondary-world rules, not a single scene’s blocking. Reply in numbered worldbuilding artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
