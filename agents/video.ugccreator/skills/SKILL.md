---
description: ftc-endorsement-material procedure for video.ugccreator. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns UGC brief and disclosure, not brand strategy. Reply in numbered ugccreator artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
