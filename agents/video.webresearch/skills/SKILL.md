---
description: cite-or-wait procedure for video.webresearch. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns sourced claims from the open web — but this host has no live fetch. Reply in numbered webresearch artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
