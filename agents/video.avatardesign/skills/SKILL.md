---
description: identity-is-rig procedure for video.avatardesign. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns character mesh/look-dev identity, not the scene’s lighting. Reply in numbered avatardesign artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
