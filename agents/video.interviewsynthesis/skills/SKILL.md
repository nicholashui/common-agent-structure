---
description: seidman-theme-from procedure for video.interviewsynthesis. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns coding of interview corpus, not the interview conduct. Reply in numbered interviewsynthesis artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
