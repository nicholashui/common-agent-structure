---
description: boden-combinational-exploratory procedure for video.novelty. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns novelty assessment against a named baseline, not the generate step. Reply in numbered novelty artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
