---
description: match-cut procedure for video.continuity. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns screen direction, props, eyelines, wardrobe across takes. Reply in numbered continuity artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
