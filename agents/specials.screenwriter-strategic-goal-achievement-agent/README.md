# specials.screenwriter-strategic-goal-achievement-agent

CASOPS v3 import of `C:\Project\common-agent-swarm-ops\business\specials\agents\specials.screenwriter-strategic-goal-achievement-agent` as `baseline_safe`.
Local deterministic adapter only. Not production-certified.

# `specials.screenwriter-strategic-goal-achievement-agent`

> Self-contained **draft** specials agent for host `common-agent-swarm-ops`.

| File | Purpose |
|------|----------|
| `SPEC.md` | Full offline role definition |
| `agent_spec.json` | Host runtime binding (draft, fail-closed) |
| `sources/` | Provenance + mapping notes for audit |
| `prompts/` | Optional prompt stubs (inert prompt_reference) |
| `rubrics/` | Optional rubric stubs |

Open this folder alone — no external repo or pack `corpus/` is required.
This agent is **not** production-active.
