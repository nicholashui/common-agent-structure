---
description: idea-must-survive procedure for video.creativedirector. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns campaign idea and brand-story lock, not DoP lighting. Emit THESIS + WHY. ASK_HUMAN when a lock is missing. Do not invent human-specified moles. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
