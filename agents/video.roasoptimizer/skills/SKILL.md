---
description: roas-window procedure for video.roasoptimizer. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns return-on-ad-spend accounting, not creative. Reply in numbered roasoptimizer artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
