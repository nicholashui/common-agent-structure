# Template baseline_safe agent

Public `agent_id`: `casops.template.baseline_safe` (folder `_template_v3`). Role: `BaselineSafeTemplate`.

Mission: exercise host compose and run with mandatory safety, corrigibility, and audit controls only.

## Domain knowledge (research)

This is the empty fail-closed contract, not a domain expert. See `sources/study/domain_knowledge.md`.
