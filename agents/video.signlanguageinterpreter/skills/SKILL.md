---
description: signing-space procedure for video.signlanguageinterpreter. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns interpreter frame: size, contrast, lag, signing space. Reply in numbered signlanguageinterpreter artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
