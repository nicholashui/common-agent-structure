---
description: cite-or-wait procedure for video.factchecker. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns claim → evidence → rating, not the news voice. Reply in numbered factchecker artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
