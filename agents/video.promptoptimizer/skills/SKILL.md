---
description: ape-promptbreeder-are procedure for video.promptoptimizer. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns iterative prompt diffs against a rubric, not first-write. Reply in numbered promptoptimizer artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
