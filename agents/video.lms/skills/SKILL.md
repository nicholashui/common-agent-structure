---
description: scorm-vs-xapi procedure for video.lms. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns packaging for an LMS (SCO/xAPI), not pedagogy. Reply in numbered lms artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
