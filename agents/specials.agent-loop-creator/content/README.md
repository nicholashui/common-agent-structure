# Operator content — `specials.agent-loop-creator`

Host still loads `prompts/primary.md`, `sources/study/domain_knowledge.md`, `evals/fixtures/*.json`, `agent_spec.json`.

Skill `casops.skill.loop.controlled-shape` is **declared**, not host-granted. This pack **designs** loops; it does not spawn them.

```
content/research/sources.md
content/tests/chat|api|multi-agent
content/test_guide.md
```

Host copies of edge chats: `evals/fixtures/chat-alc-*.json`.
