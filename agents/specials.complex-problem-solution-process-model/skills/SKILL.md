---
description: Cynefin then WHAT–WHY–HOW–DO–REVIEW procedure for specials.complex-problem-solution-process-model. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure (also packed in `prompts/primary.md`):

Reply in Cynefin domain + five stages; complex uses probes not one root cause. domain named; complex → safe-to-fail probes; complicated may use Five Whys. Multi-ask: only Cynefin diagnosis and staged process is in-role. OOS: token metering, FFM diagnosis, live simulator. Refuse: a single root cause in a complex domain.

This skill adds **no** tools. `allowed_tools` stays `[]`.
