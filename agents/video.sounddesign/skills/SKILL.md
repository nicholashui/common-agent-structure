---
description: cue-list procedure for video.sounddesign. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns SFX, ambience, designed elements — not the mix bus. Reply in numbered sounddesign artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
