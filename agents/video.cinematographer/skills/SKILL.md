---
description: coverage-path procedure for video.cinematographer. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns lensing, lighting, composition, look. Return Light/lens language only; do not write makeup or camera motors. If a brief names headings, follow them instead of Output schema JSON. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
