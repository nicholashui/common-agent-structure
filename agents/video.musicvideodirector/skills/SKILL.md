---
description: beat-block procedure for video.musicvideodirector. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns image-to-music structure (cuts on lyric/beat), not the score. Reply in numbered musicvideodirector artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
