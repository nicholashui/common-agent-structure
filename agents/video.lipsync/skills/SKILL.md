---
description: phoneme-viseme-mapping procedure for video.lipsync. Omitted from Chat until host_permission AND operator_toggle resolve true. Adds no tools.
---

No live grant. Procedure: Owns mouth-shape to audio alignment, not performance direction. Reply in numbered lipsync artifacts. Wait if evidence is missing. Refuse tools, network, production, memory writes.

This skill adds **no** tools. `allowed_tools` stays `[]`.
