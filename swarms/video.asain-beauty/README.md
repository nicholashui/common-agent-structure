# video.asain-beauty

CHARACTERIZATION roster of the asain-beauty Project Chat spine. Not a live v4 swarm runner.

`POST /api/v3/swarms/video.asain-beauty/runtime/run` is **not implemented**. Control UI `?swarm=video.asain-beauty` filters Fleet / Org Chat / Main SVG clicks to this roster. Project Chat remains `casops.project.v1` host collab.
