# Using Files

Open Agent Profile → Files. Pick a folder, then a file. Fill Actor / Reason, then Save (or Preview save in dry-run). Existing tabs such as Memory, Plugins, Protocols, Safety, and Corrigibility still show host slices, not the raw folder tree. Editing a file here does not run plugins, grant network, enable T3, or write memory records.
