# Run

Host-observe / DAG run for the selected agent. Dry-run is a compact control on this page. Default dry-run still executes the local DAG. Model nodes use the same Chat adapter (`host_llm` or `grok_acp`) and packed folder context. Does not write memory, enable plugins, T3, or network.
